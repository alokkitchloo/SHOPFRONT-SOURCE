# docs

Put your Lighthouse report screenshot here before submitting, e.g. `lighthouse.png`.

To generate one:
1. `npm run build && npm run preview`
2. Open the preview URL in Chrome
3. DevTools → Lighthouse tab → generate report (Mobile + Desktop)
4. Screenshot the summary scores and save it in this folder
